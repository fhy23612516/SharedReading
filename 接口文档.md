# 一起阅读 API 接口约束

本文档面向后续小程序迁移。当前网页端通过前端服务代理访问 `/api`，小程序端可以直接请求同一套后端接口。

## 基础约定

1. 默认后端地址：`http://127.0.0.1:3210`
2. 公网访问时：由前端服务或网关代理到后端 `/api`
3. 请求格式：`Content-Type: application/json`
4. 身份字段：推荐使用 `Authorization: Bearer <token>`；旧网页端仍兼容 `userId` 和 `name`
5. 房间身份：后端会校验 token 用户或 `userId` 是否属于房间成员
6. 消息幂等：发送消息建议带 `clientId`，用于弱网重试去重

## 通用错误

| 错误码 | 含义 |
| --- | --- |
| `not_found` | 接口不存在 |
| `room_not_found` | 房间不存在 |
| `not_room_member` | 当前身份不是房间成员 |
| `not_room_owner` | 当前身份不是房主 |
| `room_full` | 房间已满 |
| `room_ended` | 房间已结束或已关闭 |
| `invalid_progress` | 进度值非法 |
| `empty_message` | 消息为空 |
| `message_too_long` | 消息过长 |
| `unauthorized` | 未登录或 token 无效 |
| `invalid_credentials` | 账号或密码错误 |
| `account_exists` | 账号已存在 |
| `invalid_account` | 账号格式非法 |
| `weak_password` | 密码长度不足 |
| `invalid_feedback_type` | 反馈类型非法 |
| `feedback_content_required` | 反馈内容为空 |
| `feedback_too_long` | 反馈内容过长 |
| `payload_too_large` | 请求体过大，导入场景通常需要拆分正文 |
| `invalid_json` | JSON 请求体解析失败 |
| `book_title_required` | 导入书籍缺少书名 |
| `book_content_too_short` | 导入正文太短 |
| `book_content_too_long` | 导入正文太长 |
| `query_required` | 搜索关键词为空 |
| `story_not_found` | 阅读内容不存在或当前用户不可见 |
| `invalid_comment_scope` | 评论范围非法 |
| `invalid_paragraph_index` | 段落序号非法 |
| `comment_content_required` | 评论内容为空 |
| `comment_too_long` | 评论内容过长 |

## 接口列表

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/bootstrap` | 获取内容列表、进度参考线、快捷消息 |
| `GET` | `/api/records` | 获取最近共读记录 |
| `POST` | `/api/session` | 创建或恢复用户身份 |
| `POST` | `/api/auth/register` | 注册自有账号 |
| `POST` | `/api/auth/login` | 登录自有账号 |
| `GET` | `/api/auth/me` | 获取当前登录用户 |
| `POST` | `/api/auth/logout` | 退出登录并失效 token |
| `POST` | `/api/feedback` | 登录用户提交反馈 |
| `GET` | `/api/feedback/mine` | 查看我的反馈 |
| `POST` | `/api/books/import` | 登录用户导入书籍 |
| `GET` | `/api/books/mine` | 查看我导入的书籍 |
| `GET` | `/api/search` | 搜索内置和当前账号导入的内容 |
| `GET` | `/api/bookshelf` | 查看我的书架 |
| `POST` | `/api/bookshelf` | 加入书架 |
| `POST` | `/api/bookshelf/remove` | 移出书架 |
| `GET` | `/api/reading/history` | 查看我的浏览记录 |
| `POST` | `/api/reading/history` | 写入或更新浏览记录 |
| `GET` | `/api/stories/:storyId/comments` | 查看章节评论或段落评论 |
| `POST` | `/api/stories/:storyId/comments` | 发布章节评论或段落评论 |
| `POST` | `/api/rooms` | 创建房间 |
| `POST` | `/api/rooms/join` | 通过房间码加入房间 |
| `GET` | `/api/rooms/:roomId` | 获取房间详情 |
| `GET` | `/api/rooms/:roomId/events` | 建立 SSE 实时连接 |
| `POST` | `/api/rooms/:roomId/presence` | 上报在线心跳 |
| `POST` | `/api/rooms/:roomId/progress` | 上报阅读进度 |
| `POST` | `/api/rooms/:roomId/messages` | 发送聊天消息 |
| `POST` | `/api/rooms/:roomId/complete` | 标记自己读完 |
| `POST` | `/api/rooms/:roomId/leave` | 离线/返回首页 |
| `POST` | `/api/rooms/:roomId/close` | 房主关闭房间 |

## 关键请求体

### 创建或恢复身份

```json
{
  "userId": "可选，已有身份时传",
  "name": "昵称"
}
```

### 创建房间

```json
{
  "userId": "user-xxx",
  "name": "昵称",
  "storyId": "story-xxx",
  "threshold": 8
}
```

### 加入房间

```json
{
  "userId": "user-xxx",
  "name": "昵称",
  "code": "123456"
}
```

### 上报进度

```json
{
  "userId": "user-xxx",
  "name": "昵称",
  "progress": 42.5
}
```

### 发送消息

```json
{
  "userId": "user-xxx",
  "name": "昵称",
  "clientId": "local-message-id",
  "content": "消息内容"
}
```

如果已经登录，创建房间、加入房间、上报进度、发送消息等写接口可以不再传 `userId` 和 `name`，改用统一请求头：

```http
Authorization: Bearer session-token
```

## SSE 事件

| 事件名 | 含义 |
| --- | --- |
| `room` | 房间全量状态，进入房间或房间关闭/完成时使用 |
| `progress` | 某个用户的进度变化 |
| `message` | 新聊天消息 |
| `presence` | 成员上线、离线、返回 |
| `ping` | 保活事件 |

## 小程序迁移注意事项

1. 小程序不能直接依赖浏览器 `localStorage`，需要使用小程序本地存储保存 `userId`、`roomId` 和离线队列。
2. 如果小程序环境不支持标准 `EventSource`，需要改用 WebSocket 或轮询。
3. 公网部署时必须使用 HTTPS 域名，并在小程序后台配置合法请求域名。
4. 当前后端已经支持自有账号 token；小程序端可以用同一套账号接口登录，不强制微信/QQ 登录。

## 当前后端作为小程序后端的判断

当前 `server.js` 可以作为小程序后端的基础版本使用，普通 API 可以被小程序通过 `wx.request` 调用。
账号、反馈、房间和进度接口已经统一在 `/api` 下，生产环境推荐让 Nginx 把 `/api/` 直接代理到 `127.0.0.1:3210`。

建议的小程序调用域名形式：

```text
https://你的域名/api/...
```

正式迁移前建议补充：

1. HTTPS 域名和小程序后台合法域名配置。
2. 小程序端 token 本地存储和失效处理。
3. 如果需要实时体验，优先评估 WebSocket；如果先做 MVP，可以用 1-2 秒轮询房间详情。
4. 服务器按需启用 MySQL，步骤见 `MySQL迁移与启用.md`。

完整服务器配置和迁移记录见 `服务器配置记录与小程序迁移.md`。

## 已实现接口：统一账号与反馈

Web 和小程序共用同一套自有账号系统，不强制使用微信/QQ 登录。

### 注册

```text
POST /api/auth/register
```

```json
{
  "account": "fhy123",
  "password": "12345678",
  "nickname": "小明"
}
```

### 登录

```text
POST /api/auth/login
```

```json
{
  "account": "fhy123",
  "password": "12345678"
}
```

### 当前用户

```text
GET /api/auth/me
```

```http
Authorization: Bearer session-token
```

### 退出登录

```text
POST /api/auth/logout
```

```http
Authorization: Bearer session-token
```

### 提交反馈

```text
POST /api/feedback
```

```json
{
  "type": "bug",
  "content": "手机端聊天区显示异常",
  "contact": "可选联系方式"
}
```

### 我的反馈

```text
GET /api/feedback/mine
```

### 导入书籍

导入书籍需要登录账号。

```text
POST /api/books/import
```

```http
Authorization: Bearer session-token
```

```json
{
  "title": "我的故事",
  "author": "可选作者",
  "tags": "小说 睡前读物",
  "summary": "可选简介",
  "text": "第一段正文。\n\n第二段正文。"
}
```

导入限制：

1. `title` 必填，最多 80 字符。
2. `author` 最多 40 字符，不填时后端使用“用户导入”。
3. `summary` 最多 160 字符。
4. `tags` 最多保留 8 个标签，支持空格、英文逗号和中文逗号分隔。
5. `text` 会按空行拆段，正文去除空白后至少 30 个字。
6. 单本文本上限约 50 万字符。
7. `/api/books/import` 的请求体上限约 2.5MB，超过会返回 `payload_too_large`。
8. Web 导入页支持 `UTF-8` 和 `GBK / GB18030` TXT 解码；API 本身只接收已经解码后的 `text`。

返回：

```json
{
  "book": {
    "id": "book_xxx",
    "title": "我的故事",
    "author": "可选作者",
    "cover": "导入书籍",
    "summary": "可选简介",
    "body": ["第一段正文。", "第二段正文。"],
    "wordCount": 12,
    "source": "imported",
    "tags": ["小说", "睡前读物"]
  }
}
```

导入后的书会出现在当前账号的 `/api/bootstrap` 内容列表里，创建房间时直接把 `book.id` 作为 `storyId` 传给 `/api/rooms`。

### 我的导入书籍

```text
GET /api/books/mine
```

### 搜索

搜索会返回内置内容和当前登录账号可见的导入内容。

```text
GET /api/search?q=关键词
```

返回字段会带上当前账号的书架状态、最近阅读进度和评论数量摘要：

```json
{
  "items": [
    {
      "id": "story-xxx",
      "title": "故事标题",
      "author": "作者",
      "summary": "简介",
      "wordCount": 1200,
      "source": "builtin",
      "tags": ["短篇"],
      "inBookshelf": true,
      "lastProgress": 42.5,
      "commentSummary": {
        "chapter": 3,
        "paragraphs": {
          "0": 2
        }
      }
    }
  ]
}
```

### 书架

书架需要登录账号，Web 和小程序共用同一套 token。

```text
GET /api/bookshelf
POST /api/bookshelf
POST /api/bookshelf/remove
```

加入书架：

```json
{
  "storyId": "story-xxx"
}
```

### 浏览记录

浏览记录需要登录账号。进入房间和上报进度时，后端会自动更新浏览记录；小程序也可以主动写入。

```text
GET /api/reading/history
POST /api/reading/history
```

主动写入：

```json
{
  "storyId": "story-xxx",
  "roomId": "room-xxx",
  "progress": 36.8
}
```

### 章节评论和段落评论

查看章节评论：

```text
GET /api/stories/story-xxx/comments?scope=chapter
```

查看某个段落评论：

```text
GET /api/stories/story-xxx/comments?scope=paragraph&paragraphIndex=0
```

发布评论需要登录：

```text
POST /api/stories/story-xxx/comments
```

章节评论：

```json
{
  "scope": "chapter",
  "content": "这一章节奏很好"
}
```

段落评论：

```json
{
  "scope": "paragraph",
  "paragraphIndex": 0,
  "content": "这段很有画面感"
}
```

完整设计见 `后续迭代设计-账号反馈MySQL性能.md`。
